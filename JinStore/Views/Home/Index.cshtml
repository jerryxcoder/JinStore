@{
    ViewBag.Title = "Home Page";
}
@using JinStore.Models
@model Product

@{
    List<SelectListItem> PassengerNum = new List<SelectListItem>();
    PassengerNum.Add(new SelectListItem
    {
        Text = "1",
        Value = "1"
    });
    PassengerNum.Add(new SelectListItem
    {
        Text = "2",
        Value = "2"

    });
    PassengerNum.Add(new SelectListItem
    {
        Text = "3",
        Value = "3"
    });
    PassengerNum.Add(new SelectListItem
    {
        Text = "4",
        Value = "4"
    });
    PassengerNum.Add(new SelectListItem
    {
        Text = "5",
        Value = "5"
    });
    PassengerNum.Add(new SelectListItem
    {
        Text = "6",
        Value = "6"
    });
}
<div class="jumbotronCustom">

    <div class="jumbotron container body-content">
        <div ng-controller="TripsController" class="container">
            <div ng-init="getTrips()"></div>
            @using (Html.BeginForm("SearchResult", "Products", FormMethod.Get))
            {
                <h2>Flight Search</h2>
                <label>Origin</label>
                <div class="form-group input-group-sm">                  
                    <select ng-model="departureCity" id="origin" name="origin">
                        <option ng-repeat="city in trips.trips.data.city" value="{{city.code}}">{{city.name}}</option>
                        <option ng-repeat="airport in trips.trips.data.airport" value="{{airport.code}}">{{airport.name}}</option>
                    </select>
                </div>
                <label>Destination</label>
                <div class="form-group input-group-sm">                   
                    <select ng-model="arrivalCity" id="destination" name="destination">
                        <option ng-repeat="city in trips.trips.data.city" value="{{city.code}}">{{city.name}}</option>
                        <option ng-repeat="airport in trips.trips.data.airport" value="{{airport.code}}">{{airport.name}}</option>
                    </select>
                </div>

                <div class="form-group input-group-sm">
                    <label>Departure Date</label>
                    @Html.TextBoxFor(model => model.departureTime, new { @class = "form-control datepicker", placeholder = "Enter Departure date here..." })
                    @Html.ValidationMessageFor(model => model.departureTime)
                </div>

                @*<div class="form-group input-group-sm">
                    @Html.LabelFor(model => model.ReturnDate)
                    @Html.TextBoxFor(model => model.ReturnDate, new { @class = "form-control datepicker", placeholder = "Enter Return date here..." })
                    @Html.ValidationMessageFor(model => model.ReturnDate)
                </div>*@

                <div class="form-group input-group-sm">
                    @Html.DropDownListFor(model => model.adultCount, PassengerNum, "-- Select Number of Passenger --")
                </div>
                <input type="submit" value="Search" class="btn btn-primary" />
            }         

        </div>
    </div>
</div>
@section scripts{
    <script type="text/javascript">
        $(function () { // will trigger when the document is ready
            $('.datepicker').datepicker(); //Initialise any date pickers
        });
    </script>
}
<div class="container body-content">
    <div class="row">
        <div class="col-md-4">
            <h2>About ZipLine</h2>
            <p>
                ZipLine is a ecommerce website that allow users to search <b>Real Time</b> flight information, 
                ,purchase flight tickets and manage custoer database.
            </p>
            <p><a class="btn btn-default" href="http://localhost:54097/Home/Contact">Learn more &raquo;</a></p>
        </div>
        <div class="col-md-4">
            <h2>ZipLine Technologies</h2>
            <p>
                ZipLine is built in <b>ASP.Net Framework</b>, technlogies includes but not limited to <b>
    Entity framework, SQL Express, Angular JS, Bootstrap,
    JavaScript, HTML and CSS</b>
            </p>
            <p><a class="btn btn-default" href="http://localhost:54097/Home/Contact">Learn more &raquo;</a></p>
        </div>
        <div class="col-md-4">
            <h2>Web APIs</h2>
            <p>ZipLine intergrate <b>Google QPX API, Braintree API,
                SmartyStreetAPI, SendGrid</b> to retrieve and validate realtime information</p>
            <p><a class="btn btn-default" href="http://localhost:54097/Home/Contact">Learn more &raquo;</a></p>
        </div>
      
    </div>
</div>
